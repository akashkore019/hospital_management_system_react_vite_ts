import { Box, Typography } from '@mui/material';

const Appointments = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Appointments</Typography>
      <Typography>Schedule and manage appointments here.</Typography>
    </Box>
  );
};

export default Appointments;
