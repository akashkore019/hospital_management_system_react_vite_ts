import { Box, Typography } from '@mui/material';

const Settings = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Settings</Typography>
      <Typography>Configure system preferences here.</Typography>
    </Box>
  );
};

export default Settings;
